# @hpcc-js/wasm-zstd（vendored）

出典: https://www.npmjs.com/package/@hpcc-js/wasm-zstd
バージョン: 1.13.4（`dist/index.js` を無改変でコピー）

wasm を内包した自己完結の単一 ESM ファイル。外部 fetch 不要でブラウザから直接
`import { Zstd } from "./index.js"` できる。ライセンスは配布元パッケージに準ずる。

更新するときは同パッケージの新しい `dist/index.js` で本ファイルを丸ごと置き換える。
