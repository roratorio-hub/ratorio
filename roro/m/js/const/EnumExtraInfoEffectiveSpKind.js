/**
 * EnumExtraInfoEffectiveSpKind の定数定義.
 *
 * !!! 自動生成ファイル。手で編集しない !!!
 * 生成: node util/enum/gen-const-modules.mjs
 * 値の一次情報: util/enum/enum-values.snapshot.json
 *
 * 値は旧 CGlobalConstManager.DefineEnum が実行時に採番していたものを凍結したもの。
 * **値を変えるとセーブデータとアイテムデータの解釈が壊れる**ため、
 * 変更時は必ず node util/enum/verify-enum-values.mjs を通すこと。
 */

// ---- 列挙定数 ----
export const EXTRA_INFO_EFFECTIVE_SP_KIND_NONE           = 0;
export const EXTRA_INFO_EFFECTIVE_SP_KIND_STATUS_BASIC   = 1;
export const EXTRA_INFO_EFFECTIVE_SP_KIND_STATUS_SPECIAL = 2;
export const EXTRA_INFO_EFFECTIVE_SP_KIND_STATUS_EXTRA   = 3;
export const EXTRA_INFO_EFFECTIVE_SP_KIND_STATUS_ALL     = 4;
