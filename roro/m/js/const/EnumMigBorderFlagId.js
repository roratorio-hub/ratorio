/**
 * EnumMigBorderFlagId の定数定義.
 *
 * !!! 自動生成ファイル。手で編集しない !!!
 * 生成: node util/enum/gen-const-modules.mjs
 * 値の一次情報: util/enum/enum-values.snapshot.json
 *
 * 値は旧 CGlobalConstManager.DefineEnum が実行時に採番していたものを凍結したもの。
 * **値を変えるとセーブデータとアイテムデータの解釈が壊れる**ため、
 * 変更時は必ず node util/enum/verify-enum-values.mjs を通すこと。
 */

// ---- 列挙定数 ----
export const MIG_BORDER_FLAG_ID_NONE  = 0;
export const MIG_BORDER_FLAG_ID_EQUAL = 1;
export const MIG_BORDER_FLAG_ID_NOT   = 2;
export const MIG_BORDER_FLAG_ID_UNDER = 3;
export const MIG_BORDER_FLAG_ID_OVER  = 4;
export const MIG_BORDER_FLAG_ID_BY    = 5;

// ---- 疑似定数（旧 DefinePseudoEnum） ----
export const MIG_BORDER_FLAG_ID_COUNT = 6;
export const MIG_BORDER_FLAG_ID_ANY   = 7;
